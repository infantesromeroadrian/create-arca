---
description: "[Shadowed by the web3-audit skill] Smart contract security audit — 10 DeFi bug classes, pre-dive kill signals, grep patterns, Foundry PoC template. Canonical logic lives in the skill (skills take precedence over commands). Usage: /web3-audit <contract.sol>"
---

# /web3-audit

This command is **shadowed** by the `web3-audit` skill, which Claude Code
invokes with precedence over commands of the same name. The full, canonical
methodology — 10 bug classes, pre-dive kill signals, per-class grep patterns,
the Foundry PoC template, and real paid Immunefi examples — lives there:
`skills/web3-audit/SKILL.md` (553 lines, the superset).

Kept as a thin pointer instead of a divergent 215-line duplicate
(CMD-001, config audit 2026-06-09).
